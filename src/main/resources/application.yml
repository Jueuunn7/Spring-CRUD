spring:
  application:
    name: spring-curd

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/hello-spring
    username: root
    password: @@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8081